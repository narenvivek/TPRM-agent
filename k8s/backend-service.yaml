apiVersion: v1
kind: Service
metadata:
  name: tprm-backend
  namespace: tprm-agent
  labels:
    app: tprm-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: tprm-backend
